[Unit]
Description=Snappy Proxy
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a snappy-proxy
ExecStop=/usr/bin/docker stop -t 2 snappy-proxy

[Install]
WantedBy=multi-user.target